# Tasks

- [ ] Update `H2` component to spread props to the `h2` element.
- [ ] Refactor `TextCard` to accept `reverse` prop and implement Flex/Grid ordering.
- [ ] Update `TextCard` to generate `aria-labelledby` and pass `id` to `H2`.
- [ ] Update `index.astro` to use `reverse={true}` instead of `[direction:rtl]`.
- [ ] Verify visual layout matches original design (Image Left/Text Right).
- [ ] Verify accessibility tree shows correct labelling.
