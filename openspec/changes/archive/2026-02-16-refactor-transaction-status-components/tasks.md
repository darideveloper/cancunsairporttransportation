# Tasks

- [x] Import `SectionHeading.astro` and `ButtonCta.tsx` in `src/components/organisms/TransactionStatus.astro`.
- [x] Replace `<h1>` and description `<p>` with `<SectionHeading>`.
- [x] Replace `<a>` tags for actions with `<ButtonCta>`.
- [x] Verify the layout and styling matches the previous implementation using `astro check` and visual inspection.
