# Tasks

- [x] Wrap `PrivateDetails` table in a container with `overflow-x-auto`. `<!-- id: 0 -->`
- [x] Apply `min-w-[800px]` (or similar) to the table to ensure scrolling trigger. `<!-- id: 1 -->`
- [x] Implement `sticky left-0` on the first `th` and `td` (Arrival column). `<!-- id: 2 -->`
- [x] Add `z-index` and background opacity to sticky cells to handle content overlay. `<!-- id: 3 -->`
- [x] Add visual shadow cue (`after:` pseudo-element) to the sticky column for better UX. `<!-- id: 4 -->`
- [x] Verify responsiveness on mobile and desktop widths (don't open the editor, just double check the code). `<!-- id: 5 -->`
