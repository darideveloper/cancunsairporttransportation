# Tasks: Create 404 Page

1.  **Create 404 Component**
    - [x] Create `src/pages/404.astro`.
    - [x] Import `Layout`, `Logo`, `ButtonCta`.
    - [x] Set up the basic page structure.

2.  **Implement Content & Styling**
    - [x] Add `Layout` with `seo` slot (Title: "404 Not Found").
    - [x] Add a centered section using Tailwind utility classes.
    - [x] Add `Logo` (large or standard size).
    - [x] Add `h1` "Page Not Found".
    - [x] Add `ButtonCta` with "Go Back" text and `history.back()` logic.

3.  **Verify & Test**
    - [x] Build locally and test hitting a non-existent URL.
    - [x] Verify the "Go Back" button works.
    - [x] Check mobile responsiveness (handled by layout and container).
