# Tasks: Add Green Branding and Button Variants

- [x] Add green color token to global styles <!-- id: 0 -->
  - Define `--color-green: #2e8e1f` in `src/styles/global.css` within the `@theme` block.
- [x] Extend ButtonCta types and variants <!-- id: 1 -->
  - Add `"green"` and `"greenGhost"` to the `variant` type definition in `src/components/atoms/ButtonCta.tsx`.
  - Add the corresponding Tailwind classes to the `variants` object in `src/components/atoms/ButtonCta.tsx`.
- [x] Validate changes <!-- id: 2 -->
  - Verify that the new color and variants do not break existing button styles.
