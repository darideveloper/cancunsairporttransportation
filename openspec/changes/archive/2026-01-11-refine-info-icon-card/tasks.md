# Tasks: Refine InfoIconCard

1. Add `TitleIcon` and `BottomIcon` to `Props` interface in `src/components/molecules/InfoIconCard.astro`. [x]
2. Update `InfoIconCard.astro` template to render `TitleIcon` next to the `h3` title. [x]
3. Update `InfoIconCard.astro` template to render `BottomIcon` at the bottom of the `article`. [x]
4. Update spec `transportation-services-section` to include these new optional icon requirements. [x]
5. Verify the rendering with dummy icons in a test or by updating `TransportationServices.astro`. [x]
