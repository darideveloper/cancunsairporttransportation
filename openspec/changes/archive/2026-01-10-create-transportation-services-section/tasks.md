# Tasks

- [x] Add translation keys for Transportation Services (Title, Description, Items) in `en.json` and `es.json`.
- [x] Create `TransportationServices.astro` organism:
    - [x] Retrieve `lang` and `t` internally.
    - [x] Use `SectionHeading`.
    - [x] Map through service items.
- [x] Create `ServiceCard.astro` molecule (HTML structure, empty classes, React Icons).
- [x] Add `TransportationServices` to `src/pages/[lang]/index.astro` (at the bottom of the page, after the FAQ section).
- [x] Verify HTML structure and SEO attributes via `openspec validate`.
