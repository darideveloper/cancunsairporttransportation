---
// Components
import TopBar from "../molecules/TopBar.astro";
import MenuBar from "./MenuBar.astro";

interface Props {
  pageKey?: string;
  translatedPath?: string;
}

const { pageKey, translatedPath } = Astro.props;
---

<header class="mb-6">
  <TopBar pageKey={pageKey} translatedPath={translatedPath} />
  <MenuBar pageKey={pageKey} />
</header>
