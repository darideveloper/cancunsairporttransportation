---
import { useTranslations } from "../../lib/i18n/utils";
import SectionHeading from "../molecules/SectionHeading.astro";
import InfoIconCard from "../molecules/InfoIconCard.astro";
import {
  FaPlane,
  FaBed,
  FaUtensils,
  FaTree,
  FaArrowDown,
} from "react-icons/fa";

const { lang } = Astro.params as { lang: string };
const t = useTranslations(lang as "en" | "es");

const services = [
  {
    title: t(
      "global.sections.transportationServicesList.items.airportToHotel.title",
    ),
    text: t(
      "global.sections.transportationServicesList.items.airportToHotel.text",
    ),
    Icon: FaPlane,
  },
  {
    title: t(
      "global.sections.transportationServicesList.items.hotelToHotel.title",
    ),
    text: t(
      "global.sections.transportationServicesList.items.hotelToHotel.text",
    ),
    Icon: FaBed,
  },
  {
    title: t(
      "global.sections.transportationServicesList.items.hotelToRestaurant.title",
    ),
    text: t(
      "global.sections.transportationServicesList.items.hotelToRestaurant.text",
    ),
    Icon: FaUtensils,
  },
  {
    title: t(
      "global.sections.transportationServicesList.items.hotelToPark.title",
    ),
    text: t(
      "global.sections.transportationServicesList.items.hotelToPark.text",
    ),
    Icon: FaTree,
  },
];

const title = t("global.sections.transportationServicesList.title");
const description = t("global.sections.transportationServicesList.description");
const id = "transportation-services";
---

<section
  class="relative container mx-auto mt-12 px-4 py-12"
  id={id}
  aria-label={title}
>
  <div
    class="absolute top-0 left-1/2 -z-10 h-[60%] w-screen -translate-x-1/2 bg-[#fff9f2]"
  >
  </div>

  <div class="mb-16 flex flex-col items-center justify-center text-center">
    <div class="flex max-w-3xl flex-col items-center justify-center gap-6">
      <SectionHeading title={title} class="font-bolder text-center">
        {
          description && (
            <p class="mt-4 text-justify !text-sm text-lg text-gray-600 md:text-left md:text-xl">
              {description}
            </p>
          )
        }
      </SectionHeading>
    </div>
  </div>

  <div class="grid grid-cols-2 gap-8 md:grid-cols-2 lg:grid-cols-4">
    {
      services.map((item) => (
        <InfoIconCard
          title={item.title}
          text={item.text}
          BottomIcon={item.Icon}
        />
      ))
    }
  </div>
</section>
