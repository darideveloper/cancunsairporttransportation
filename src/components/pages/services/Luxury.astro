---
// Layout
import Layout from "../../../layouts/Layout.astro";

// Libs
import { useTranslations } from "../../../lib/i18n/utils";

// Components
import PageSEO from "../../../components/utils/PageSEO.astro";
import BookingForm from "../../../components/organisms/BookingForm";
import PricingSection from "../../../components/organisms/PricingSection.astro";
import Testimonials from "../../../components/organisms/Testimonials.astro";
import FaqSection from "../../../components/organisms/FaqSection.astro";
import Includes from "../../../components/organisms/Includes.astro";
import PrivateDetails from "../../../components/organisms/PrivateDetails.astro";

// Images
import homeBannerImg from "../../../assets/images/home-banner.webp";
import luxuryImg from "../../../assets/images/vehicles/luxury.webp";
import luxuryClient1Img from "../../../assets/images/clients/luxury-1.webp";
import luxuryClient2Img from "../../../assets/images/clients/luxury-2.webp";
import luxuryClient3Img from "../../../assets/images/clients/luxury-3.webp";

// Page data
// Props
interface Props {
  lang: "en" | "es";
  pageKey: string;
}
// Get language from params or props
const { lang, pageKey } = Astro.props;

// Page data
const contentKey = pageKey || "luxury";

// Tranlation texts
const t = useTranslations(lang);
---

<Layout preloadImage={homeBannerImg.src} pageKey={pageKey}>
  <!-- Page SEO -->
  <PageSEO
    currentPage={contentKey}
    slot="seo"
    jsonType="Service"
    ogImage={luxuryImg.src}
  />

  <!-- Form -->
  <div class="@container" id="booking-form">
    <BookingForm
      translations={t("global.booking")}
      client:idle
      title={t(`pages.${contentKey}.title`)}
    />
  </div>

  <!-- Service Includes -->
  <Includes contentKey={contentKey} image={luxuryImg} />

  <!-- Private Details -->
  <PrivateDetails contentKey={contentKey} />

  <!-- Clients testimonials -->
  <Testimonials
    contentKey={contentKey}
    images={[luxuryClient1Img, luxuryClient2Img, luxuryClient3Img]}
  />

  <!-- FAQs -->
  <FaqSection contentKey={contentKey} />

  <!-- Pricing transportation -->
  <PricingSection contentKey={contentKey} isRegularGrid />
</Layout>
