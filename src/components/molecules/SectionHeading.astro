---
interface Props {
  title: string;
  class?: string;
  id?: string;
}

const { title, class: className = "", id } = Astro.props;
---

<div class={className}>
  <h2 class="text-2xl font-bold" id={id}>
    {title}
  </h2>
  <div class="mt-4 text-sm">
    <slot />
  </div>
</div>
